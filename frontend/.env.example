# Optional legacy backend API for non-JLD screens still present in the repo.
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Dedicated Supabase project for Jean Louis David Mexico.
# Copy these values into frontend/.env.local once the new JLD project exists.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
